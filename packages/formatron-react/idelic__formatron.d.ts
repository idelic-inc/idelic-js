declare module '@idelic/formatron' {
  export interface DataInterface {
    // Define the properties and methods of DataInterface
  }

  export interface DataType {
    // Define the properties and methods of DataType
  }

  export class FormatronClass {
    constructor(dataInterface: DataInterface, dataTypes?: DataType[]);
    // Define the properties and methods of Formatron
  }
}
